---
app:
  # Persistence
  persistence:
    jpa:
      generate-ddl: true
      show-sql: true
    datasource:
       url: jdbc:mariadb://127.0.0.1:3307/uris?ssl=false
  # Swagger
  swagger:
    enabled: true
    secured: false
#    authServerUrl: ../oauth
#    authServerClientId: acme
#    authServerClientSecret: acmesecret

## Actuator
management:
  health:
    redis:
      enabled: false
  server:
    port: 9326
